<h1>Tasks Index</h1>

  <% @tasks.each do |task| %>
    <div class='task_<%= task.id %>'>
      <h3><a href = '/tasks/<%= task.id %>'><%= task.name %></a></h3>
      <h3><%= task.description %></h3>
    </div>
  <% end %>
